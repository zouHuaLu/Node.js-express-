//OS模块，提供基本的系统操作函数
const os = require('os')
//Path模块，提供了处理和转换文件路的工具
const path = require('path')
//Net模块，用于底层的网络通信，提供了服务端和客户端的操作
const net = require('net')
//DNS模块，用于解析域名
const dns = require('dns')
//Domain模块，可以捕捉处理try catch无法捕捉的
const domain = require('domain')
